The following LaTeX document failed to compile. Fix ONLY what is needed to make it compile cleanly, changing as little as possible — do not restyle, reword, or restructure the content.

## Compiler error
```
{{error}}
```

## LaTeX document
```latex
{{latex}}
```

## Rules
1. Make the minimal edit that resolves the error. The most common cause is an unescaped special character in the prose — escape it (`&`→`\&`, `%`→`\%`, `#`→`\#`, `$`→`\$`, `_`→`\_`). Do not touch `%` characters that begin comments.
2. Do NOT change any wording, numbers, names, dates, or LaTeX structure beyond the fix.
3. Preserve the full document.

## Output
Return the COMPLETE corrected LaTeX document and nothing else — no markdown code fences, no commentary. Start at `\documentclass` (or the leading `% !TEX` line) and end at `\end{document}`.
